"use client";
import '../catalog/catalog.css';
import Link from 'next/link';
import {useMemo,useState} from 'react';
import {clothingCatalog} from '../../lib/clothing';
export default function CatalogPage(){const [category,setCategory]=useState('All');const items=useMemo(()=>category==='All'?clothingCatalog:clothingCatalog.filter(x=>x.category===category),[category]);return <main className="catalog-page"><nav className="ww-nav"><Link href="/" className="ww-logo"><span className="ww-logo-mark">W</span>WEARWISE</Link><Link href="/#finder" className="back-link">← Style Finder</Link></nav><section className="dashboard-hero"><span className="section-label">WEARWISE CLOSET</span><h1>Explore the<br/><em>pieces.</em></h1><p>Browse the visual clothing catalog used by the recommendation system.</p><div className="finder-options">{['All','Top','Bottom','Shoes'].map(x=><button key={x} className={category===x?'finder-option active':'finder-option'} onClick={()=>setCategory(x)}>{x}</button>)}</div></section><section className="catalog-grid">{items.map(item=><article className="catalog-card" key={item.id}><img src={item.image} alt={item.name}/><div><small>{item.category}</small><h2>{item.name}</h2><p>{item.styles.join(' · ')}</p></div></article>)}</section></main>}
