"use client";
import Link from 'next/link';
import {useState} from 'react';
export default function UploadPage(){const [file,setFile]=useState(null);const [submitted,setSubmitted]=useState(false);return <main className="upload-page"><nav className="ww-nav"><Link href="/" className="ww-logo"><span className="ww-logo-mark">W</span>WEARWISE</Link><Link href="/dashboard" className="back-link">Dashboard →</Link></nav><section className="upload-workspace"><span className="section-label">OUTFIT REVIEW</span><h1>Rate your<br/><em>look.</em></h1><p>Choose a photo of an outfit and WearWise will prepare it for structured style feedback. This version keeps your image in the browser and does not upload it to a server.</p><label className="upload-drop"><input type="file" accept="image/*" onChange={e=>{setFile(e.target.files?.[0]||null);setSubmitted(false)}}/><span className="upload-plus">+</span><strong>{file?file.name:'Choose outfit photo'}</strong><small>{file?'Ready for review':'JPG, PNG or WEBP'}</small></label>{file&&<button className="finder-submit" onClick={()=>setSubmitted(true)}>Analyze Outfit ↗</button>}{submitted&&<div className="feedback-result"><span className="section-label">PREVIEW FEEDBACK</span><h2>Strong starting point.</h2><p>WearWise can evaluate color coordination, silhouette balance, occasion suitability and overall styling. Connect an image-analysis provider later for automated visual scoring.</p></div>}</section></main>}
